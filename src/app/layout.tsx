import type { Metadata } from "next";
import { Playfair_Display, Lato } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"],
});

const lato = Lato({
  variable: "--font-lato",
  subsets: ["latin"],
  weight: ["300", "400", "700", "900"],
});

export const metadata: Metadata = {
  title: "Daniel Isibor & CO | Chartered Accountants",
  description: "Daniel Isibor & CO (Chartered Accountants) provides premium auditing, tax management, financial advisory, and accounting software consultancy. Based in Abuja, Nigeria.",
  keywords: ["chartered accountant", "auditing services", "tax management", "financial reporting", "accounting software consultancy", "Abuja accountant", "Daniel Isibor & CO", "DIC"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${playfair.variable} ${lato.variable} h-full scroll-smooth antialiased`}
    >
      <body className="min-h-full flex flex-col bg-slate-50 text-slate-900">{children}</body>
    </html>
  );
}
